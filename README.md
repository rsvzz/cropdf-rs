# Libcropdf is a library to generate pdf

## Libcropdf currently under development

#### libcropdf is written in rust implementing Cairo libraries to generate pdf.


## Requirements

    rustc-1.92
    cairo 0.22.0

## Install
    cargo build --release